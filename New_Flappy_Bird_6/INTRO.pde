void intro() {
  textAlign(CENTER);

  textSize(32);
  fill(textColor);
  text("Press SPACE to start", width/2, height*0.75);

  textSize(75);
  fill(black);
  text("New Flappy Bird", width/2, height*0.3);
}
